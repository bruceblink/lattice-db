# Lattice DB 项目级 Codex 开发规则

本文件是 Lattice DB 仓库的项目级 Codex 指令。需求边界以 `docs/requirements-and-consistency-v0.1.md` 为准，开发顺序和验收证据以 `docs/development-plan-v0.1.md` 为准。

## 实现语言

- 核心存储、事务、日志、恢复、同步、游标和冲突处理统一使用 Rust stable。
- 阶段 0 必须在 `rust-toolchain.toml` 中冻结具体 stable toolchain、MSRV、目标平台和必要组件；在该文件出现前，不开始实现运行时核心代码。
- 首期不使用 Go、C++、Zig 或其他语言实现核心运行时。其他语言只能在后续有明确边界和独立验收条件时用于工具或 SDK。

## 阶段交付

- 每个阶段和阶段子单元都必须是完整的可运行单元，不得只交付能编译的库、接口声明或未接通的占位实现。
- 每个阶段必须提供固定运行入口、最小端到端样例、固定测试数据、可核验输出和独立验收命令。
- 阶段按依赖顺序逐个推进；前一阶段未完成构建、运行、测试和验收，不进入下一阶段。
- 后续阶段必须保留前序阶段的运行入口和回归测试，并在其基础上增加能力。
- 阶段 0 也必须有可运行的规范验证入口，至少检查协议字段、版本号、容量参数、错误码、状态转换和故障矩阵；未冻结项必须返回非零退出码。
- 每个独立可验收阶段或切片只允许一次独立提交；测试通过后立即推送当前功能分支，不合并或推送 `main`。

## 关键模块测试覆盖率

- 每个阶段必须登记关键代码模块。关键模块包括直接决定日志编解码、校验和、恢复、提交序列、索引、快照、事务、游标、去重、冲突判定、资源上限和错误分类的生产代码。
- 关键模块必须达到 100% 单元测试覆盖率。覆盖率口径为函数、行/语句、区域和分支均为 100%。集成测试、属性测试、模糊测试、手工测试和示例运行不能抵扣单元覆盖率缺口。
- 覆盖率统一使用 Rust LLVM 覆盖率工具链（例如 `cargo llvm-cov`），报告必须绑定当前提交、Rust toolchain、目标系统和完整测试命令。
- 错误处理、边界检查、容量限制、版本拒绝、磁盘错误、重试终止和状态拒绝分支必须由单元测试直接覆盖。
- 生成代码、测试辅助代码和纯启动装配代码只有在覆盖率清单中逐项登记路径及原因后才能排除；不得通过扩大排除范围或删除未覆盖代码绕过要求。
- 任一关键模块低于 100%，或覆盖率工具不可用、报告无法对应当前提交时，阶段保持未完成。

## 代码规模限制

- 单个 Rust 代码文件最多 600 行，单个函数或方法最多 120 行；生产代码、单元测试、集成测试和示例代码统一适用。
- 行数按提交后的物理源代码行统计，空行、注释和宏展开前的源码也计入；函数或方法从签名起始行计算到主体结束行。
- 超过限制时必须拆分模块、提取职责或拆分测试数据；不得通过压缩多条语句、宏隐藏实现或关闭检查绕过限制。
- 生成代码、第三方源码和明确的外部接口兼容层可以排除，但必须在检查配置中登记路径、生成来源和排除原因；项目自行维护的生产逻辑不得排除。
- 阶段验收和提交前检查必须运行自动化源码规模检查，并报告超限文件、函数、实际行数和对应提交。任一超限都视为验证失败，不得提交为完成状态。

## 提交前验证

从 workspace 根目录执行并通过：

```text
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets
关键模块 100% 覆盖率检查
源码文件和函数规模检查
```

根据阶段范围追加崩溃恢复、故障注入、本机 Docker 多节点和 UI/CLI 验收。Docker 集成测试只能使用本机 Docker，并记录服务、镜像版本、端口、启动状态、故障注入方式和清理状态。任何测试失败、覆盖率不足、集成环境不可用或工作树混入无关修改，都不得提交为完成状态。

## 文档和代码边界

- 修改需求、架构、协议或阶段验收时，先更新对应文档，再实现代码。
- 非平凡 Rust 函数添加简短函数级注释，说明职责、关键输入/输出和重要副作用；不添加逐行无意义注释。
- 保持文件使用 LF 换行。
- 提交信息使用英文 Conventional Commit 前缀，例如 `feat:`, `fix:`, `test:`, `docs:` 或 `chore:`。
